define(function() {
    var assembleCSS = {
        buildStylesheet: function(params) {
            console.log("Building stylesheet from parameters: " + params);
        }
    }
    return assembleCSS;
});